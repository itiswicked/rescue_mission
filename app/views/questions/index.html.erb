<h3>Questions</h3>

<ul class='questions'>
  <% @questions.each do |question| %>
    <li>
      <%= question.title %>
      <%= question.created_at.strftime("%b %-d, %Y @ %l:%M %p") %>
    </li>
  <% end %>
</ul>
